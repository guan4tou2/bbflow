# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-swagger-spec-exposed

info:
  name: OpenAPI/Swagger JSON spec exposed (real spec, catch-all aware)
  author: hermes-bb-kb
  severity: low
  description: |
    Real OpenAPI/Swagger spec (version string) discloses full API surface; excludes Swagger UI HTML / SPA shell. KB: OpenAPI Swagger Spec Info Disclosure.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/swagger.json"
      - "{{BaseURL}}/swagger/v1/swagger.json"
      - "{{BaseURL}}/openapi.json"
      - "{{BaseURL}}/v2/api-docs"
      - "{{BaseURL}}/v3/api-docs"
      - "{{BaseURL}}/api-docs"
      - "{{BaseURL}}/api/swagger.json"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '"swagger":"2'
          - '"swagger": "2'
          - '"openapi":"3'
          - '"openapi": "3'
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
          - '<html'
